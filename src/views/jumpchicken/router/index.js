import Vue from 'vue';
import VueRouter from 'vue-router';



// 路由配置
const routes = [
  // {
  //   name: 'login',
  //   path: '/login',
  //   component: Login,
  // },
]

Vue.use(VueRouter);

const router = new VueRouter({
  routes
});
export default router;
